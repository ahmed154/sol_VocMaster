<div class="container ml-4">
    <div class="row">
        <div class="col-auto"> <span style="font-size:30px;color:transparent">&emsp;&emsp;&emsp;&emsp;&nbsp;</span></div>

        <div class="col">

            @if (Voc.VocsIdioms != null && Voc.VocsIdioms.Count > 0)
            {
                <div class="row mt-4">
                    <div class="col">

                        <div class="row float-right">
                            <a href="/idiom/new">Add Idiom</a>
                        </div>
                        <div class="row ml-1 mt-4">
                            <table>

                                @foreach (var vocIdiom in Voc.VocsIdioms)
                                {
                                    <tr style="height:40px; color:navy; font-family:'Tajawal'; font-weight: bold; font-size: 16px;">@((MarkupString)vocIdiom.Idiom.Text)</tr>                                   
                                }

                            </table>
                        </div>
                    </div>
                </div>          
            }
        </div>
    </div>
</div>

@code
{
    [Parameter] public Voc Voc { get; set; }
    [Inject] public IJSRuntime JSRuntime { get; set; }

    protected override Task OnInitializedAsync()
    {

        return base.OnInitializedAsync();
    }
}

<style>
    .quoteStyle {
        border: none;
        border-bottom: 1px solid #eeeeee;
    }
</style>